# Assets

Place static assets here:
- Profile photo (recommended: 400x400px, optimized)
- Favicon files
- Any images referenced in index.html

Keep assets optimized — large images slow down deploys and page loads.
The pipeline deploys everything in `site/` to production, so only commit what should be public.
